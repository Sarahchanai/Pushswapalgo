#include "push_swap.h"

/*int     main(int argc, char **argv)
{
    int *stack_a = NULL;
    int size;
    int i = 0;
    
    size = parse_arguments(argc, argv, &stack_a);
    
    if (size == -1)
    {
        printf("Error : Invalid input or invalid number\n");
        return (1);
    }
    else if (size == 0)
    {
        printf("No arguments provided\n");
        return (0);
    }
    else
    {
        printf("Parsing successfull !\n");
        printf ("Size of stack : %d\n", size);
        while (i < size)
	    {
		    printf("stack[%d] = %d\n", i, stack_a[i]);
		    i++;
	    }
	    free(stack_a);
	    return (0);

    }
}*/